'use strict';

describe('myApp.dosecalc module', function() {

  beforeEach(module('myApp.dosecalc'));

  describe('dosecalc controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var dosecalcCtrl = $controller('DosecalcCtrl');
      expect(dosecalcCtrl).toBeDefined();
    }));

  });
});