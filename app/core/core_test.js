'use strict';

describe('myApp.core module', function() {

  beforeEach(module('myApp.core'));

  describe('core controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var coreCtrl = $controller('CoreCtrl');
      expect(core).toBeDefined();
    }));

  });
});